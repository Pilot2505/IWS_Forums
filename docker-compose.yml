version: "3.9"

services:

  server:
    build: ./server
    ports:
      - "3000:3000"
    env_file:
      - ./server/.env

  client:
    build: ./client
    ports:
      - "8080:8080"
    depends_on:
      - server